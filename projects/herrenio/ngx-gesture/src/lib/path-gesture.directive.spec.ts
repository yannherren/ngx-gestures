import { PathGestureDirective } from './path-gesture.directive';

describe('PathGestureDirective', () => {
  it('should create an instance', () => {
    const directive = new PathGestureDirective();
    expect(directive).toBeTruthy();
  });
});
