import { TapGestureDirective } from './tap-gesture.directive';

describe('TapGestureDirective', () => {
  it('should create an instance', () => {
    const directive = new TapGestureDirective();
    expect(directive).toBeTruthy();
  });
});
